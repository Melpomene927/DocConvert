---
description: 定義 AI 作為資深 Windows 軟體工程師，協助開發和管理 Windows 腳本
globs: ["*.ps1", "*.bat", "*.cmd"]
alwaysApply: true
---

# 代碼風格
- 所有檔案必須使用 ANSI 編碼，以確保中文字符正確顯示
- 
## PowerShell
- 使用動詞-名詞命名慣例
- 使用完整的參數名稱
- 實現管道操作
- 使用進階函數

## Batch Script
- 使用有意義的標籤名稱
- 實現錯誤級別檢查
- 使用環境變數
- 實現條件分支


# PowerShell 腳本 (.ps1)
## 命名規範
- 使用 PascalCase 命名檔案
- 函數名稱使用動詞-名詞格式
- 變數使用有意義的名稱，避免縮寫

## 結構規範
- 檔案開頭必須包含腳本說明、版本、作者資訊
- 參數區塊使用 [CmdletBinding()] 屬性
- 使用 Begin-Process-End 區塊結構
- 實作 -WhatIf 和 -Verbose 參數支援

## 編碼與格式
- 使用 2 個空格進行縮排
- 使用 #region 和 #endregion 組織代碼區塊

## 錯誤處理
- 使用 try-catch 處理可預期的錯誤
- 實作 ErrorAction 參數
- 使用 Write-Error 輸出錯誤訊息
- 適當使用 -ErrorVariable 參數

## 日誌記錄
- 使用 Write-Verbose 記錄詳細執行過程
- 使用 Write-Debug 記錄除錯資訊
- 使用 Write-Warning 記錄警告訊息
- 實作日誌檔案輸出機制

# Batch 腳本 (.bat/.cmd)
## 命名規範
- 使用小寫字母和底線命名檔案
- 標籤名稱使用描述性文字
- 變數名稱使用大寫字母

## 結構規範
- 檔案開頭設置環境變數 (SETLOCAL)
- 包含版本資訊和使用說明
- 主要邏輯之前進行參數驗證
- 結尾清理環境變數 (ENDLOCAL)

## 編碼與格式
- 使用 2 個空格進行縮排
- 重要區塊使用 REM 或 :: 註解標記
- 使用 ECHO 輸出空行分隔不同區段

## 錯誤處理
- 使用 IF ERRORLEVEL 檢查錯誤
- 實作錯誤訊息輸出
- 錯誤發生時執行清理程序
- 提供錯誤碼說明

## 執行控制
- 使用 CALL 執行子程序
- 使用 START 執行外部程式
- 正確處理路徑中的空格
- 使用 CHOICE 或 SET /P 處理使用者輸入

# 共同規範
## 安全性
- 避免在腳本中存放敏感資訊
- 使用環境變數傳遞敏感資料
- 執行完畢後清理暫存資料
- 限制檔案存取權限

## 效能考量
- 避免不必要的外部命令呼叫
- 使用內部命令替代外部命令
- 適當使用批次處理減少 I/O 操作
- 注意迴圈效能優化

## 相容性
- 注意 Windows 版本差異
- 檢查必要工具是否存在
- 提供相容性警告訊息
- 實作替代方案支援
